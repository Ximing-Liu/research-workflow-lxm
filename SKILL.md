---
name: research-workflow
description: 用 Agent 协作做科研的完整工作流——建项目脚手架、抓相关文献（PDF+LaTeX 双格式）、复现 baseline、提假设、跑实验、管上下文交接。任何涉及开新课题、搭科研项目、找/读/整理论文、复现别人的方法、设计或跑实验、记录实验结果、写实验交接文档的请求都用它——包括用户只说「开个新坑」「帮我把这几篇论文弄下来」「这个 baseline 跑一下」「实验记一下」「快没上下文了」「接着上次的做」而没提「科研」两个字的时候。也用于判断一个实验设计有没有漏洞（比如结果突然变好该怀疑什么）。
---

# 科研工作流

这套流程的核心判断：**AI 能接管科研里的绝大部分工程量，但发现问题和提出假设这一步接管不了。**
所以工作流的设计目标不是"让 AI 做完"，而是**把该自动化的彻底自动化，把人的注意力省下来投到那一步**。

## 五个阶段，顺序很重要

```
1. related_work/     抓文献          ← 可以整个交给 Agent
2. 复现/              跑通关键 baseline ← 可以整个交给 Agent
3. brainstorm.md     发现问题 + 提假设  ← 人主导，Agent 陪聊
4. experiment/       写代码跑实验      ← Agent 主力，人 verify 关键节点
5. DataSet/          数据处理          ← 可以整个交给 Agent
```

**阶段 2 在阶段 3 之前，这是反直觉但关键的一点。**

论文只报平均值和成功的配置。它卡在哪、哪个 setting 崩了、报的数字复现不出来——
这些**只有自己跑一遍才看得见**，而这些正是好问题的来源。
先复现再找问题，跟"先读完文献再想 idea"是两种效率完全不同的路径。

如果用户上来就说"帮我想个 idea"，先问他复现过没有。没复现过的话，
比起硬想，不如先挑两三篇重要的跑通——通常问题会自己浮出来。

## 开新项目

```bash
bash <skill>/scripts/init-project.sh <项目目录>
```

建出这套结构，模板在 `assets/templates/`：

| 文件 | 作用 | 谁在读 |
|---|---|---|
| `AGENTS.md` | 项目总纲：在做什么、每个目录干什么、有哪些硬规矩 | **每个新 Agent 第一个读** |
| `handoff.md` | 进度交接：现在到哪了、下一步、踩过的坑 | **每个新 Agent 第二个读** |
| `brainstorm.md` | 问题和假设是怎么聊出来的 | 想不通设计意图时 |
| `related_work/` | 文献，每篇 `paper.pdf` + `source/` | 要引用、要对比时 |
| `复现/` | baseline 复现 + 复现报告 | 要确认 baseline 数字时 |
| `experiment/` | 实验代码 + `results.md` + `evaluation.md` | 跑实验时 |
| `DataSet/` + `dataset.md` | 数据和处理说明 | 动数据时 |

在已有项目里被调起时，**先读 `AGENTS.md` 和 `handoff.md` 再动手**，
不要为了"了解全貌"把整个仓库读一遍——上下文是这个流程里最稀缺的资源。

## 抓文献

```bash
bash <skill>/scripts/fetch-paper.sh <arxiv-id> [目录名]
```

**PDF 和 LaTeX 源码都下**，因为两者各有各的用处，不是二选一：

- **泛读**（这篇讲了什么、用了什么方法）→ 喂 `paper.pdf`，更省 token
- **细查**（表里那个数、公式怎么推、引的是哪篇）→ 读 `source/*.tex`，准确率不是一个量级

脚本会自动隔离作者打包进来的旧稿、剥掉注释掉的正文、提示哪些文件装着自定义宏。
**这三件事不做，Agent 会引用到已经被推翻的数字。**

有实测数据支撑这个路由规则，也有几个反直觉的点（比如 LaTeX 并不省 token）——
**要跟用户解释为什么这么做，或者要判断某个具体场景该读哪个，先读
[references/reading-papers.md](references/reading-papers.md)。**

## 复现

新建 `复现/<方法名>/`，让 Agent 跑通。**只挑有开源代码的**——
没有开源代码的，Agent 会照着论文描述"猜"一个实现，猜出来的东西不能当 baseline。

跑完写一份 `复现报告.md`，三件事：

1. **论文报的数字 vs 你跑出来的**，对不上就查，别糊弄过去
2. **踩了什么坑**（超参默认值和 README 不一致这类事非常常见）
3. **⭐ 论文没报但你看见了的东西** —— 这一条最值钱

第 3 条的做法：论文报平均，你就按维度切开看分布；论文报最好配置，你就把差的也跑一遍。
**gap 藏在方差里，不在均值里。**

## 提假设

假设不能直接问 Agent 要，准确率不够。但**可以聊出来**——
你给一个粗糙的方向，让它帮你找反例、帮你设计怎么证伪、帮你把约束条件说清楚。
往往是"我拿不到 X"这类约束，逼出了真正的设计。

单开一个上下文专门聊这个，过程记进 `brainstorm.md`，**倒序写，最新的在最上面**。
把被自己否掉的想法也留着——那是后面写 motivation 的素材。

## 跑实验

这一阶段 Agent 是主力，但有三件事人必须做。展开见
[references/experiment-discipline.md](references/experiment-discipline.md)，简版：

**一、verify 结构，也 verify 中间过程。**
现在的模型很快也很莽，不等指令就往前迈一大步。看代码结构（不用细，但要知道它在干什么），
再逐块问它输入输出数据怎么处理的、配合代码对一遍。**小问题基本都在中间过程里。**

**二、每一版都命名、都留着。**
一篇文章要迭代八九版。`results.md` 里一版一行，不覆盖不删。
命名用领域里已有的词，禁止 `test2` / `final_v2`。

**三、测评标准化。**
`evaluation.md` 写死用什么数据训测、报哪些指标、出哪些可视化。
之后一句"用标准化测评测一下"就够了。**这份文档的作用是防止 cherry-pick。**

> **一条值钱的直觉**：某一版效果突然暴涨，第一反应应该是**查数据泄漏**，不是庆祝。
> 最常见的是 split 按样本分而不是按类别/来源分，训练和测试撞了。

## 数据

Agent 时代最省事的一块——看 bad case、下数据、配网络代理都能交给它。
处理过程和命名规则写进 `dataset.md`，方便下一个 Agent 直接用。

下大数据集时两个命令能省很多事：

```bash
caffeinate -i          # Mac 不休眠，否则合盖就断
```

```
/loop 下载 X 到 Y，停止条件是校验通过，断了自动续传
```

## 管上下文

一篇实验做下来往往要三十多个窗口，再大的上下文也会爆。

**每个窗口结束前更新 `handoff.md`**，写三件事：现在到哪了、下一步做什么、
这轮踩了什么坑。写给"完全不知道前情的下一个 Agent"看。

上下文快满时主动提醒用户更新 handoff，**不要硬撑到被截断**——
被截断时丢失的往往正是刚刚调试出来的关键结论。

## 硬规矩（写进项目的 AGENTS.md）

1. **不要自己发明术语。**解释在做什么时用论文里已有的词。
   自造术语会让人看不懂你在干什么，这是失控的开始。
2. **改实验前先说要改什么、为什么**，不要直接动手跑。
3. **效果突然变好先查泄漏。**
4. **窗口快满了主动说**，别硬撑。
